//first go through the items
//but even even before then, need to link to the data

var data = require ('../products.json')

var items = data.items
var amtOfItems = items.length
console.log('there are ' + amtOfItems + ' items')
//objects of array of list?
